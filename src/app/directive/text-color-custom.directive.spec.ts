import { TextColorCustomDirective } from './text-color-custom.directive';

describe('TextColorCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new TextColorCustomDirective();
    expect(directive).toBeTruthy();
  });
});
