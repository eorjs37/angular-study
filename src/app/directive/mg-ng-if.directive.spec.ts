import { MgNgIfDirective } from './mg-ng-if.directive';

describe('MgNgIfDirective', () => {
  it('should create an instance', () => {
    const directive = new MgNgIfDirective();
    expect(directive).toBeTruthy();
  });
});
