import { TextBuleDirective } from './text-bule.directive';

describe('TextBuleDirective', () => {
  it('should create an instance', () => {
    const directive = new TextBuleDirective();
    expect(directive).toBeTruthy();
  });
});
